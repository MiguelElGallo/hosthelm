apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "superset_snowf.fullname" . }}-test-connection"
  labels:
{{ include "superset_snowf.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "superset_snowf.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
